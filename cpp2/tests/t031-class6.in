class Deck {
    int score;
    //Deck d;
    //Player p;
    void Init() {score = 0;}
	void Shuffle() {Print("Shuffle");}
	Deck GetDeck() {return this;}
}

class Player {
	Deck d;
	void Init(Deck dj) { this.d = dj; dj = d;}
	Deck GetDeck() { return d;}
}

void main()
{
	Player p;
	int score;
	Deck deck;
	p = new Player;
	deck = p.d;
	p.Init(new Deck);
	deck.Init();
	//p.d.score;
	//deck = p.GetDeck().score[1];
	deck = p.d.GetDeck().score[1];
	//deck = p.d.d.GetDeck().p.d;
}