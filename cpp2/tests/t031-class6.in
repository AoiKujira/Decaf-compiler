class Deck {
    int score;
    //Deck d;
    //Player p;
    void Init() {score = 0;}
	void Shuffle() {Print("Shuffle");}
	Deck GetDeck() {return this;}
}

class Player {
	Deck d;
	void Init(Deck dj) { d = dj; dj = d;}
	Deck GetDeck() { return d;}
	int GetScore() { d = d.shuffle(); return d.score;} //should be corrected
}

void main()
{
	Player p;
	int score;
	int[] gool;
	Deck deck;
	string goose;
	p = new Player;
	deck = p.d;
	score = 5;
	gool[6] = 76;
	goose = "adfa";
	p.Init(new Deck);
	deck.Init();
	//p.d.score;
	//deck = p.GetDeck().score[1];
	deck = p.d.GetDeck().score[1];
	//deck = p.d.d.GetDeck().p.d;
}