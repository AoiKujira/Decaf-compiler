class Deck {
    int score;
	void Shuffle() {Print("Shuffle");}
}

class Player {
	Deck d;
	void Init(Deck dj) { d = dj;}
	Deck GetDeck() { return d;}
}

void main()
{
	Player p;
	int score;
	Deck deck;
	p = new Player;
	//deck = p.d;
	p.Init(new Deck);
	score = p.GetDeck().GetDeck().score;
}